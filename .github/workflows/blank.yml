name: Update Instructions

on:
  push:
    paths:
      - 'instructions.txt'

jobs:
  update-instructions:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Update instructions
        run: |
          # Replace this with your script to update the instruction file
          # For example, echo a new instruction into the file
          echo "New instruction" > instructions.txt

      - name: Commit changes
        run: |
          git config --local user.email "zeeshan.ijaz@emumba.com"
          git config --local user.name "ZeeshanIB"
          git commit -am "Update instructions [skip ci]"

      - name: Push changes
        uses: ad-m/github-push-action@master
       # with:
        #  github_token: ${{ secrets.GITHUB_TOKEN }}
